--AURASTREAMABLE
_G.SilentAim = {
    --[[MAIN]]
    AimingMode = "Index", -- "Namecall" or "Index" || CANNOT BE CHANGED MID GAME 
    Key = "q",
    AimParts = {"All"},
    -- Aimparts = {"Head","UpperTorso","HumanoidRootPart","LowerTorso","LeftHand","RightHand","LeftLowerArm","RightLowerArm","LeftUpperArm","RightUpperArm","LeftFoot","LeftLowerLeg","LeftUpperLeg","RightLowerLeg","RightFoot","RightUpperLeg"},
    MainPart = "HumanoidRootPart",
    KeyMode = "Toggle", -- "Hold",
    Prediction = 0.1259910,
    AutoPrediction = false,
    Notifications = false,
    --[[RESOLVER (EXPERIMENTAL)]]
    ResolverV1 = true, -- false uses HumanoidRootPart/Torso velocity | true uses ResolverAimpart velocity 
    ResolverDelay = 0.1259910,
    ResolverAimpart = "HumanoidRootPart",
    AntiSky = true,
    AntiGround = true,
    ReversePrediction = false,
    --[[SELECTION]]
    FOVCheck = true, -- js check the closet player on ur cursor
    FOVColor = Color3.fromRGB(0,100,0),
    FOVSize = 120,
    FOVThickness = 2,
    FOVFilled = false,
    FOVTransparency = 1,
    DistancePriority = true,
    VisibleCheck = false,
    DownCheck = false,
    --[[VISUALS]] -- BTW THESE JUST SHOW U WHERE ITS AIMING
    Dot = false,
    DotColor = Color3.fromRGB(0,100,0),
    Tracer = false,
    TracerColor = Color3.fromRGB(0,100,0),
    Highlight = false,
    HighlightColor = Color3.fromRGB(0,100,0),
    --[[EXTRAS]]
    RandomizeShots = false,
    XRandomization = 500, -- x,y, and z are all divided by 1000 btw so its not gonna completely fucking shoot at the sky if u do 500
    YRandomization = 500,
    ZRandomization = 500,
    --[[LEAVE THESE ALONE UNLESS U KNOW WHAT UR DOING (literally nothing complicated)]]
    Enabled = true,
    AllowAim = true,
}
loadstring(game:HttpGet("https://raw.githubusercontent.com/AURACREATOR/AuraFOVStreamable/main/FOVSTREAMABLE", true))()
